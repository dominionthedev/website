---
---

<section class="stage-section" data-stage="stack" data-title="Stack" data-align="right">
  <p class="label">Stack</p>
  <h2>What I actually reach for.</h2>
  <div class="stack-list">
    <p class="stack-line" data-typed data-typed-text="Languages — Go, Python, Rust, Zig, Lua"></p>
    <p class="stack-line" data-typed data-typed-text="Environment — WezTerm, Tmux, Neovim, zsh"></p>
  </div>
</section>

<style>
  .stack-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    margin-top: var(--space-4);
    align-items: flex-end;
  }

  .stack-line {
    font-size: var(--text-terminal);
    color: var(--color-signal);
    min-height: 1.5em;
  }
</style>
