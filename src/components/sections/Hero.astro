---
---

<section class="stage-section" data-stage="hero" data-title="Start" data-align="center">
  <h1 class="hero-thesis" data-typed data-typed-text="I live in the terminal"></h1>
  <p class="hero-sub" data-reveal>A tour, not a resume.</p>
</section>

<style>
  .hero-thesis {
    font-size: var(--text-display);
    min-height: 1.2em;
  }

  .hero-thesis::after {
    content: "_";
    color: var(--c-green);
    animation: blink 1.1s step-end infinite;
    margin-left: 2px;
  }

  .hero-sub {
    font-family: var(--font);
    font-size: var(--text-terminal);
    color: var(--c-mute);
    margin-top: var(--space-3);
  }
</style>
